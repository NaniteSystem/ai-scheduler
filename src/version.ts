/**
 * Canonical product version shown in the interface. Keep this in sync with
 * package.json and Android versionName whenever a user-approved release is cut.
 */
export const APP_VERSION = '1.4.0';
